# MQTT broker connection. Defaults target a local, unauthenticated broker for development
# (e.g. `brew services start mosquitto`). Override these in other environments.
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# QoS applied to both the subscription and published commands.
MQTT_QOS=1